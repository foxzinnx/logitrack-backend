export enum PackageStatus{
    PENDING = 'PENDING',
    AWAITING_PICKUP = 'AWAITING_PICKUP',
    PICKED_UP = 'PICKED_UP',
    IN_TRANSIT = 'IN_TRANSIT',
    DELIVERED = 'DELIVERED',
    RETURNED = 'RETURNED'
}

export class Package{
    private constructor(
        
    ){}
}